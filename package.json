{
  "name": "ai-browser-tester",
  "version": "0.1.0",
  "description": "An agent-first browser testing tool — optimized for LLM agents, not test scripts",
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "abt": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "tsx src/cli.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "playwright": "^1.48.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0"
  }
}
